'use strict';

describe('myApp.slide module', function() {

  beforeEach(module('myApp.slide'));

  describe('slide controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var slideCtrl = $controller('slideCtrl');
      expect(slideCtrl).toBeDefined();
    }));

  });
});